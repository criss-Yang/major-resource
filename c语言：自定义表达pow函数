#include<stdio.h>
main(void)
{
int z,x,y;
int power(int x,int y);
printf("please input x and y\n");
scanf("%d %d",&x,&y);
z=power(x,y);
printf("%d\n",z);
}

int power(int x,int y)
{
	int z=1;
	int i,j;
	j=y;
for(i=0;i<j;i++)
	{
		z=z*x;
	}
return z;
}
